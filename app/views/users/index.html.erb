<div class="container">
	<div class="row">
		<%= render 'users/list', book: @book, user: @user %>
		<div class="col-xs-9">
			<h1>Users</h1>
			<%= render "follow_form", users: @users %>
		</div>
		</div>
	</div>
</div>
