<div class="container">
	<div class="row">
		<%= form_tag :search_users, method: :get, class:"search" do %>
			<%= text_field_tag "q", params[:q], placeholder: "search user" %>
			<%= submit_tag '検索' %>
		<% end %>

		<%= render 'users/list', book: @book, user: @user %>
		<div class="col-xs-9">
			<h1>Users</h1>
			<%= render "follow_form", users: @users %>
		</div>
		</div>
	</div>
</div>
